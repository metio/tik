{:event/actor "seb" :event/at #inst "2026-07-29T17:41:12.827Z" :event/body {:fact/path [:summary] :fact/value "stage.cljc:56 claims O(n * stages). Measured on a linear history with 8 stages: 100 events 550ms, 200 events 622ms, 400 events 1846ms, 800 events 7545ms - 4.1x per doubling. evolve runs a full reached-set fixpoint per event; each fact guard reaches fact-status then conflicting-claims then path-writes, which scans the whole log (reduce.cljc:154), plus ancestor? DAG walks. The fact-memo (guard.cljc:62) is scoped to one reached-set call, so every event redoes it. effective-reached is on every lens path, per ticket. Checkpoints are pre-rejected by PLAN 18, and sticky makes the per-event fixpoint genuinely path-dependent, so the wins have to come from a per-path write index, hoisting the memo into the evolve accumulator, and skipping the fixpoint for events touching no guarded path. Correct the complexity claim either way."} :event/parents #{"sha256-aa9b50aaafa49a444847064000e82d83ff5075320fc9d620db0663d9e71ae589"} :event/ticket #uuid "d8fe995d-a019-4821-8144-12edaeaab850" :event/type :fact/assert}